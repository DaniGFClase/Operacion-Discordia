1- Modify the application so that after adding a product it redirects
to the product table, with the same category.

2 - Make the necessary changes to encrypt the user table passwords
(password_hash () function).

3 - Modify the product table so that it does not show products without stock.

4 - Add the total weight of the order in the content of the confirmation email.

5 - Store the mail server information in an XML file as done
with that of the database. Modify the functions of sending mail to use it.

6 - Create an XSD scheme for mail configuration and validate the file with it
configuration before using it.

7 - On the cart page, add a link to empty the cart.

8 - On the cart page, add a link / button to each row to
Remove all units of that product.

